{
  "name": "speakseed-backend",
  "version": "1.0.0",
  "description": "SpeakSeed Laravel Backend",
  "scripts": {
    "build": "composer install --optimize-autoloader",
    "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
